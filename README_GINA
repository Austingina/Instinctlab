

## 概述

InstinctLab 是 Project Instinct 的环境侧仓库，基于 Isaac Lab，用于仿人机器人（如 Unitree G1）全身控制与模仿学习。本文档说明：

- ✅ **版本要求**：Isaac Sim 5.1.0、Isaac Lab 2.3.2
- ✅ **安装顺序**：Isaac Lab → instinct_rl → instinctlab
- ✅ **任务配置**：Shadowing（BeyondMimic / Whole Body / Perceptive）与 Parkour 的配置文件与命令

## 版本要求与参考

### 版本要求

| 组件 | 要求 |
|------|------|
| Isaac Sim | 5.1.0 |
| Isaac Lab | 2.3.2，推荐 commit：`37ddf626871758333d6ed89cf64ad702aef127d0`（2026-01-30） |
| Python | 3.11 |

详细安装与切换步骤见：https://zcnd7jmdi1lj.feishu.cn/sync/JtwHd7UUDsJrtGbOFC0cSy3tnEh

## 安装 Instinct-RL

### 步骤

1. **克隆仓库**（与 Isaac Lab 安装目录分开）
2. **安装**（使用已安装 Isaac Lab 的 Python）

**流程：**

```bash
git clone https://github.com/project-instinct/instinct_rl.git
cd instinct_rl
python -m pip install -e .
```

更多依赖与说明：<https://github.com/project-instinct/instinct_rl/blob/main/README.md>

## 克隆并安装 InstinctLab（本仓库）

### 步骤

1. **克隆仓库**（与 Isaac Lab 分开）
2. **安装本仓库**（必须使用已装 Isaac Lab 的 Python，在 instinctlab 根目录执行）

**流程：**

```bash
git clone https://github.com/project-instinct/instinctlab.git
cd instinctlab
python -m pip install -e source/instinctlab
```

## 验证安装

在 instinctlab 仓库根目录执行（无图形、GPU）：

```bash
python scripts/instinct_rl/train.py --task=Instinct-Shadowing-WholeBody-Plane-G1-Play-v0 --headless
```

**若报错：**
- 缺少 `instinct_rl` 或 `instinctlab`：确认使用的是安装过 Isaac Lab 的 Python
- 与 Isaac Lab / Isaac Sim 版本相关：回到「版本要求与参考」核对版本与 commit

参考：https://zcnd7jmdi1lj.feishu.cn/sync/UQDSd5T7Vs5hKtbvJFOc52wEnCc

## 各任务配置与运行

### Shadowing 任务

#### BeyondMimic（Instinct-BeyondMimic-Plane-G1-v0）

| 项目 | 说明 |
|------|------|
| 配置文件 | `source/instinctlab/instinctlab/tasks/shadowing/beyondmimic/config/g1/beyondmimic_plane_cfg.py` |
| 必设项 | `MOTION_NAME`、`_hacked_selected_file_`、`AmassMotionCfg.path` |

**训练：**
```bash
python scripts/instinct_rl/train.py --headless --task=Instinct-BeyondMimic-Plane-G1-v0
```

**播放：**
```bash
python scripts/instinct_rl/play.py --task=Instinct-BeyondMimic-Plane-G1-v0 --load_run=<run_name>
```

#### Whole Body Shadowing（Instinct-Shadowing-WholeBody-Plane-G1-v0）

| 项目 | 说明 |
|------|------|
| 配置文件 | `source/instinctlab/instinctlab/tasks/shadowing/whole_body/config/g1/plane_shadowing_cfg.py` |
| 必设项 | `MOTION_NAME`、`_path_`、`_hacked_selected_files_` |

**训练：**
```bash
python scripts/instinct_rl/train.py --headless --task=Instinct-Shadowing-WholeBody-Plane-G1-v0
```

**播放：**
```bash
python scripts/instinct_rl/play.py --task=Instinct-Shadowing-WholeBody-Plane-G1-v0 --load_run=<run_name>
```

#### Perceptive Shadowing（Instinct-Perceptive-Shadowing-G1-v0）

| 项目 | 说明 |
|------|------|
| 配置文件 | `source/instinctlab/instinctlab/tasks/shadowing/perceptive/config/g1/perceptive_shadowing_cfg.py` |
| 必设项 | `MOTION_FOLDER` |

**训练：**
```bash
python scripts/instinct_rl/train.py --headless --task=Instinct-Perceptive-Shadowing-G1-v0
```

**播放：**
```bash
python source/instinctlab/instinctlab/tasks/shadowing/play.py --task=Instinct-Perceptive-Shadowing-G1-v0 --load_run=<run_name>
```

### Parkour 任务（Instinct-Parkour-Target-Amp-G1-v0）

| 项目 | 说明 |
|------|------|
| 配置文件 | `source/instinctlab/instinctlab/tasks/parkour/config/g1/g1_parkour_target_amp_cfg.py` |
| 必设项 | `AmassMotionCfg.path`、`filtered_motion_selection_filepath` |

**训练：**
```bash
python scripts/instinct_rl/train.py --headless --task=Instinct-Parkour-Target-Amp-G1-v0
```

**播放：**
```bash
python source/instinctlab/instinctlab/tasks/parkour/scripts/play.py --task=Instinct-Parkour-Target-Amp-G1-v0 --load_run=<run_name>
```

**导出 ONNX**：在上述 play 命令后追加 `--exportonnx --useonnx`。

**说明**：以上命令均在 instinctlab 仓库根目录执行。

## 常用参数

| 参数 | 说明 |
|------|------|
| `--headless` | 无图形界面（服务器必备） |
| `--num_envs <N>` | 并行环境数 |
| `--load_run <run_name>` | 播放时加载的 run 目录（建议绝对路径） |
| `--no_resume` | 播放时不加载 checkpoint |
| `--video` | 录制训练/播放视频 |
| `--keyboard_control` | 播放时键盘控制 |

## 可选：代码格式与 IDE

### pre-commit（代码格式化）

```bash
pip install pre-commit
pre-commit run --all-files
pre-commit install   # 每次提交自动运行
```

### VSCode

- `Ctrl+Shift+P` → “Tasks: Run Task” → 选择 `setup_python_env`，按提示填写 Isaac Sim 安装路径，会在 `.vscode` 下生成 `.python.env`，便于补全与跳转。

### Pylance 找不到扩展包

- 在 `.vscode/settings.json` 的 `"python.analysis.extraPaths"` 中加入本仓库 `source/instinctlab` 的绝对路径。

## 注意事项

### 1. Python 环境

- 训练与播放必须使用**已安装 Isaac Lab 的同一 Python 环境**。

### 2. 动作数据路径

- `path`、`filtered_motion_selection_filepath`、YAML 内路径需一致且存在。
- Parkour 的 `filtered_motion_selection_filepath` 建议使用**绝对路径**，避免 FileNotFoundError。

### 3. run_name

- 播放前在 `logs/instinct_rl/<experiment_name>/` 下查看时间戳目录名，作为 `--load_run` 的值。

## 故障排除

### 问题1：缺少 instinct_rl 或 instinctlab

**症状**：ImportError 或 ModuleNotFoundError

**解决方法**：确认当前 Python 为安装过 Isaac Lab 的环境（如 conda 环境），并在该环境下重新执行 `pip install -e .` / `pip install -e source/instinctlab`。

### 问题2：No motion files found / FileNotFoundError

**症状**：训练启动时报路径或 YAML 相关错误

**解决方法**：检查对应任务的 cfg 中 `path`、`filtered_motion_selection_filepath` 及 YAML 内 `selected_files` 是否指向真实存在的目录和文件；Parkour 的 YAML 路径建议写绝对路径。

### 问题3：Isaac Lab / Isaac Sim 版本报错

**解决方法**：回到「版本要求与参考」核对 Isaac Sim 5.1.0、Isaac Lab 2.3.2 及推荐 commit，按飞书文档切换版本。

## 总结

InstinctLab 安装与使用要点：

- **安装顺序**：Isaac Lab（5.1.0 + 2.3.2）→ instinct_rl → instinctlab
- **任务配置**：按任务类型修改对应 cfg 中的 path、文件列表、YAML 路径
- **命令**：训练用 `scripts/instinct_rl/train.py`，播放与 ONNX 导出见各任务小节；所有命令在仓库根目录执行

**关键要点**：
- 版本：Isaac Sim 5.1.0、Isaac Lab 2.3.2
- 环境：使用安装过 Isaac Lab 的 Python 安装并运行 instinct_rl、instinctlab
- 路径：path 与 YAML 一致，Parkour 的 YAML 用绝对路径


